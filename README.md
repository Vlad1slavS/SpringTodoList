# Todo List Spring

REST API приложения для управления списком задач

## Описание

Приложение предоставляет CRUD API для управления задачами с поддержкой фильтрации, пагинации и сортировки.

## Технологии

- **Java 21**
- **Spring Boot 4**
- **Spring Data JPA**
- **PostgreSQL**
- **MapStruct** - маппинг DTO
- **Lombok**
- **JUnit 5 + Mockito** - тестирование

## Структура проекта

```
├── controller/       # REST контроллеры
├── service/          # Бизнес-логика
├── repository/       # JPA репозитории
├── entity/           # JPA сущности
├── dto/              # Data Transfer Objects
├── mapper/           # MapStruct мапперы
├── specification/    # JPA Specifications для фильтрации
├── enums/            # Перечисления
└── exception/        # Обработка исключений
```

## 🔧 Установка и запуск

### Требования

- Java 21 или выше
- Maven
- PostgreSQL

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Vlad1slavS/SpringTodoList.git
cd SpringTodoList
```

2. Настройте базу данных в `application.properties`:
```properties
spring.datasource.url=jdbc:postgresql://localhost:5432/todolist
spring.datasource.username=your_username
spring.datasource.password=your_password
```

3. Запустите приложение:
```bash
mvn spring-boot:run
```

Приложение будет доступно по адресу: `http://localhost:8080`

## API Endpoints

### Базовый URL: `/v1/tasks`

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/v1/tasks` | Получить все задачи (с пагинацией и фильтрацией) |
| `GET` | `/v1/tasks/{id}` | Получить задачу по ID |
| `POST` | `/v1/tasks/create` | Создать новую задачу |
| `PATCH` | `/v1/tasks/update/{id}` | Обновить задачу |
| `PATCH` | `/v1/tasks/update/status/{id}` | Обновить статус задачи |
| `DELETE` | `/v1/tasks/{id}` | Удалить задачу |

## Примеры использования

### Получить все задачи

```bash
GET /v1/tasks?page=0&size=10&sort=dueDate,asc&status=TODO
```

**Параметры запроса:**
- `page` - номер страницы
- `size` - размер страницы
- `sort` - сортировка (например: `title,asc` или `dueDate,desc`)
- `status` - фильтр по статусу (опционально): `TODO`, `IN_PROGRESS`, `DONE`

**Ответ:**
```json
{
  "content": [
    {
      "title": "Задача 1",
      "description": "Описание задачи",
      "status": "TODO",
      "dueDate": "2025-02-20"
    }
  ],
  "page": 0,
  "size": 10,
  "totalElements": 50,
  "totalPages": 5
}
```

### Получить задачу по ID

```bash
GET /v1/tasks/123e4567-e89b-12d3-a456-426614174000
```

**Ответ:**
```json
{
  "title": "Задача 1",
  "description": "Описание задачи",
  "status": "TODO",
  "dueDate": "2025-02-20"
}
```

### Создать задачу

```bash
POST /v1/tasks/create
Content-Type: application/json

{
  "title": "Новая задача",
  "description": "Описание новой задачи",
  "status": "TODO",
  "dueDate": "2025-02-20"
}
```

**Ответ:** `200 OK` + созданная задача

### Обновить задачу

```bash
PATCH /v1/tasks/update/123e4567-e89b-12d3-a456-426614174000
Content-Type: application/json

{
  "title": "Обновленное название",
  "description": "Обновленное описание"
}
```

**Ответ:** `200 OK` + обновленная задача

### Обновить статус задачи

```bash
PATCH /v1/tasks/update/status/123e4567-e89b-12d3-a456-426614174000
Content-Type: application/json

"IN_PROGRESS"
```

**Ответ:** `200 OK` + обновленная задача

### Удалить задачу

```bash
DELETE /v1/tasks/123e4567-e89b-12d3-a456-426614174000
```

**Ответ:** `204 No Content`

## Тестирование

Запуск unit-тестов:
```bash
mvn test
```
